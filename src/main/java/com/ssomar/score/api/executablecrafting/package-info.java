/**
 * Public API of the ExecutableCrafting plugin.
 * <p>
 * Entry point: {@link com.ssomar.score.api.executablecrafting.ExecutableCraftingAPI}. The implementation is
 * registered at runtime by the ExecutableCrafting plugin; use {@code ExecutableCraftingAPI.isEnabled()}
 * before accessing the manager when the dependency is optional.
 * <p>
 * For advanced recipe features (grids, inputs, results, groups...), use the rich API
 * shipped with the plugin: {@code vayk.executablecrafting.api.ExecutableCraftingAPI}.
 */
package com.ssomar.score.api.executablecrafting;
