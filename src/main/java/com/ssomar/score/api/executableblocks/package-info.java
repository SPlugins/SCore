/**
 * Public API of the ExecutableBlocks plugin.
 * <p>
 * Entry point: {@link com.ssomar.score.api.executableblocks.ExecutableBlocksAPI}. The implementation is
 * registered at runtime by the ExecutableBlocks plugin; use {@code ExecutableBlocksAPI.isEnabled()}
 * before accessing the manager when the dependency is optional.
 */
package com.ssomar.score.api.executableblocks;
