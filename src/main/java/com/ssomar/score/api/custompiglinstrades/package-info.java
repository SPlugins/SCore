/**
 * Public API of the CustomPiglinsTrades plugin.
 * <p>
 * Entry point: {@link com.ssomar.score.api.custompiglinstrades.CustomPiglinsTradesAPI}. The implementation is
 * registered at runtime by the CustomPiglinsTrades plugin; use {@code CustomPiglinsTradesAPI.isEnabled()}
 * before accessing the manager when the dependency is optional.
 */
package com.ssomar.score.api.custompiglinstrades;
